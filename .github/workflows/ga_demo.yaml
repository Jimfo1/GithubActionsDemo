name: demo workflow

run-name: Run of a demo workflow

on: 
  push:
    branches-ignore:
    - main
jobs:
  first-job:
    runs-on: windows-latest
    steps:
      - run: node --version
      - run: npm --version
